package exam;

public class Example_01 {

	/*
	 * 내부클래스
	 * - 내부클래스는 클래스 안에 만들어진 또 다른 클래스로 중첩클래스라고 한다.
	 * - 두 클래스 멤버들 간에 손쉽게 접근할 수 있다.
	 * - 불필요한 클래스를 감춰서 코드의 복잡성을 줄일 수 있다.
	 * - 내부클래스는 클래스 안에서 선언된 위치에 따라
	 * 인스턴트 클래스, 정적클래스, 지역클래스, 익명클래스로 구분
	 * 
	 * 형식)
	 * public class OutClass { 		//외부클래스
	 * 		class InnerClass {   		//내부클래스
	 * 
	 */
}
